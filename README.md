# RegistrationForm

A sample responsive registration form build with ReactJS, NextJS, Typescript and Redux

Demo: 

Install dependances:

`npm install`

Run the app locally:

`npm run dev`

Deploy to now.sh (requires https://zeit.co account)

`now`
